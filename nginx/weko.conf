# University of Yangon repository
# uyr.uy.edu.mm
server {
    listen 80;
    server_name uyr.uy.edu.mm;
    return 301 http://meral.edu.mm/uy/$request_uri;

#    location / {
#        rewrite_log on;
#        rewrite ^(.*)$ http://meral.edu.mm/uy/$1 permanent;
#    }
}

server {
    listen 443;
    ssl                  on;
    ssl_protocols        TLSv1 TLSv1.1 TLSv1.2;
    ssl_certificate /etc/nginx/server.crt;
    ssl_certificate_key /etc/nginx/server.key;

    server_name uyr.uy.edu.mm;
#    location / {
#        rewrite_log on;
#        rewrite ^(.*)$ https://meral.edu.mm/uy/$1 permanent;
#    }
    return 301 https://meral.edu.mm/uy/$request_uri;
}

# Yangon University of Economics repository
# ecor.yueco.edu.mm
server {
    listen 80;
    server_name ecor.yueco.edu.mm;
    location / {
        rewrite_log on;
        rewrite ^(.*)$ http://meral.edu.mm/yueco/$1 permanent;
    }
}

server {
    listen 443;
    ssl                  on;
    ssl_protocols        TLSv1 TLSv1.1 TLSv1.2;
    ssl_certificate /etc/nginx/server.crt;
    ssl_certificate_key /etc/nginx/server.key;

    server_name ecor.yueco.edu.mm;
    location / {
        rewrite_log on;
        rewrite ^(.*)$ https://meral.edu.mm/yueco/$1 permanent;
    }
}


# University of Mandalay repository
# umoar.mu.edu.mm
server {
    listen 80;
    server_name umoar.mu.edu.mm;
    location / {
        rewrite_log on;
        rewrite ^(.*)$ http://meral.edu.mm/um/$1 permanent;
    }
}

server {
    listen 443;
    ssl                  on;
    ssl_protocols        TLSv1 TLSv1.1 TLSv1.2;
    ssl_certificate /etc/nginx/server.crt;
    ssl_certificate_key /etc/nginx/server.key;

    server_name umoar.mu.edu.mm;
    location / {
        rewrite_log on;
        rewrite ^(.*)$ https://meral.edu.mm/um/$1 permanent;
    }
}


# Yadanabon university repository:
# oar.ydbu.edu.mm
server {
    listen 80;
    server_name oar.ydbu.edu.mm;
    location / {
        rewrite_log on;
        rewrite ^(.*)$ http://meral.edu.mm/ydbu/$1 permanent;
    }
}

server {
    listen 443;
    ssl                  on;
    ssl_protocols        TLSv1 TLSv1.1 TLSv1.2;
    ssl_certificate /etc/nginx/server.crt;
    ssl_certificate_key /etc/nginx/server.key;

    server_name oar.ydbu.edu.mm;

    location / {
        rewrite_log on;
        rewrite ^(.*)$ https://meral.edu.mm/ydbu/$1 permanent;
    }
}

upstream app_server {
  server web:5000 fail_timeout=0;
}

# meral.edu.mm
server {
    listen 80;
    server_name meral.edu.mm;
    charset utf-8;
    keepalive_timeout 60;

    client_max_body_size 500G;

    location /static {
        root /home/invenio/.virtualenvs/invenio/var/instance;
    }

    location / {
        uwsgi_pass app_server;
        include uwsgi_params;

        uwsgi_buffering off;
        uwsgi_request_buffering off;

        # fix 'upstream sent too big header' error
        uwsgi_buffer_size 32k;
        uwsgi_buffers 8 32k;
        uwsgi_busy_buffers_size 32k;


        uwsgi_param Host $host;
        uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
        uwsgi_param X-Forwarded-Proto $scheme;


    }

    location /api/records {
        uwsgi_pass app_server;
        include uwsgi_params;

        uwsgi_intercept_errors        off;
        uwsgi_ignore_client_abort     off;

        # fix 'upstream sent too big header' error
        uwsgi_buffer_size             256k;
        uwsgi_buffers                 4       128k;
        uwsgi_busy_buffers_size       256k;

        uwsgi_param Host $host;
        uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
        uwsgi_param X-Forwarded-Proto $scheme;

        client_max_body_size 1024G;
    }

    location /api/files {
      uwsgi_pass app_server;
      include uwsgi_params;

      uwsgi_buffering off;
      uwsgi_request_buffering off;

      # fix 'upstream sent too big header' error
      uwsgi_buffer_size 32k;
      uwsgi_buffers 8 32k;
      uwsgi_busy_buffers_size 32k;

      uwsgi_param Host $host;
      uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
      uwsgi_param X-Forwarded-Proto $scheme;

      client_max_body_size 1024G;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}

server {
    listen 443;
    ssl                  on;
    ssl_protocols        TLSv1 TLSv1.1 TLSv1.2;
    ssl_certificate /etc/nginx/server.crt;
    ssl_certificate_key /etc/nginx/server.key;

    server_name meral.edu.mm;
    charset utf-8;
    keepalive_timeout 5;

    client_max_body_size 500G;

    location /static {
        root /home/invenio/.virtualenvs/invenio/var/instance;
    }

    location / {

        uwsgi_pass app_server;
        include uwsgi_params;

        uwsgi_buffering off;
        uwsgi_request_buffering off;

        # fix 'upstream sent too big header' error
        uwsgi_buffer_size 32k;
        uwsgi_buffers 8 32k;
        uwsgi_busy_buffers_size 32k;

        uwsgi_param Host $host;
        uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
        uwsgi_param X-Forwarded-Proto $scheme;


    }

    location /api/records {
        uwsgi_pass app_server;
        include uwsgi_params;

        uwsgi_intercept_errors        off;
        uwsgi_ignore_client_abort     off;

        # fix 'upstream sent too big header' error
        uwsgi_buffer_size             256k;
        uwsgi_buffers                 4       128k;
        uwsgi_busy_buffers_size       256k;

        uwsgi_param Host $host;
        uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
        uwsgi_param X-Forwarded-Proto $scheme;

        client_max_body_size 1024G;
    }

    location /api/files {
      uwsgi_pass app_server;
      include uwsgi_params;

      uwsgi_buffering off;
      uwsgi_request_buffering off;

      # fix 'upstream sent too big header' error
      uwsgi_buffer_size 32k;
      uwsgi_buffers 8 32k;
      uwsgi_busy_buffers_size 32k;

      uwsgi_param Host $host;
      uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
      uwsgi_param X-Forwarded-Proto $scheme;

      client_max_body_size 1024G;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

}




